services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: wealth
      POSTGRES_USER: wealth
      POSTGRES_PASSWORD: wealth
    ports: ["5432:5432"]
    volumes: ["wealth-data:/var/lib/postgresql/data"]
volumes:
  wealth-data:
